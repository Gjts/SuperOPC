name: Quality

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install pytest
        run: python -m pip install --upgrade pip pytest

      - name: Run pytest
        run: python -m pytest SuperOPC/tests/test_session_workflow.py

      - name: Run repo health check
        run: python SuperOPC/scripts/opc_health.py --cwd SuperOPC --target repo --json

      - name: Run converter smoke test
        run: python SuperOPC/scripts/convert.py --tool all --out ./tmp-integrations
