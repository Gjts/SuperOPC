"""
SuperOPC Engine v2 — Autonomous operations layer.

Modules:
  event_bus          — Publish/subscribe event bus
  state_engine       — Structured .opc/ state management
  dag_engine         — DAG orchestration with resilient execution
  decision_engine    — Three-layer decision architecture
  cruise_controller  — Autonomous cruise modes (watch/assist/cruise)
  scheduler          — Background cron scheduler
  profile_engine     — 8-dimension developer profiling
  learning_store     — Cross-project knowledge persistence
  context_assembler  — Dynamic phase-aware context construction
  notification       — Multi-channel notification dispatcher
  intel_engine       — Codebase intelligence (queryable JSON index)
  instinct_generator — Observation-to-rule pipeline (closes the learning loop)
"""
