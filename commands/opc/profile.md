---
name: opc-profile
description: Inspect, export, or record developer-profile signals
---
# /opc-profile
开发者画像入口。
## 动作
调用 `python bin/opc-tools profile $ARGUMENTS`。
支持 `show`、`export`、`record`。
## 参数
- `$ARGUMENTS` — profile 子命令与参数
