---
name: opc-stats
description: Emit phase, plan, requirement, and git metrics for the current project
---
# /opc-stats
项目指标入口。
## 动作
调用 `python scripts/opc_stats.py $ARGUMENTS`。
输出阶段、计划、需求和 git 活动指标。
## 参数
- `$ARGUMENTS` — 可选，`--cwd <path>` 或 `--json`
